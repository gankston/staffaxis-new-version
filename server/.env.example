# Railway te da DATABASE_URL automáticamente al crear el plugin de PostgreSQL
DATABASE_URL=postgresql://user:password@host:5432/staffaxis

# Genera uno con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=cambia_esto_por_un_secreto_largo_y_aleatorio

# Token que usa StaffAdmin para endpoints de admin
# Genera uno con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_TOKEN=cambia_esto_por_otro_secreto

PORT=3000
NODE_ENV=production
